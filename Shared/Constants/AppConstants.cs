namespace dimax_front.Shared.Utils
{
    public static class AppConstants
    {
        public static readonly string DEFAULT_PAGE_NUMBER = "0"; // Usar const para valores inmutables en tiempo de compilación
        public static readonly string DEFAULT_PAGE_SIZE = "5";
        public static readonly string DEFAULT_SORT_BY = "Date";
        public static readonly string DEFAULT_SORT_DIR = "desc";
    }
}
